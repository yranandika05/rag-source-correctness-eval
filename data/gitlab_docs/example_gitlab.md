# GitLab merge requests

A merge request in GitLab is used to propose changes from one branch into another. Reviewers can comment and approve the changes.

## CI/CD pipelines

GitLab CI/CD pipelines are configured with a `.gitlab-ci.yml` file. Each job runs in a stage and can be triggered on commits, merges, or schedules.
